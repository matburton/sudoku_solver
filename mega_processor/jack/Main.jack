
class Main {

    function void main() {
        
        var Array grid;
        
        let grid = Stack.grow(Grid.gridByteSize());
        
        do Grid.initialise(grid);
        
        do Solver.setValueAt(grid, 1, 7, 3);
        
        do Solver.getAPossibilityAt(grid, 7, 3);
        
        return;
    }
}